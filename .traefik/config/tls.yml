tls:
  certificates:
    - certFile: /etc/certs/local.crt
      keyFile: /etc/certs/local.key
  stores:
    default:
      defaultCertificate:
        certFile: /etc/certs/local.crt
        keyFile: /etc/certs/local.key
